/** Bumped on each web deploy so clients can detect a new build.
 *  `scripts/stamp-build.js` rewrites this string in the exported JS bundle
 *  when APP_BUILD_ID is set (CI uses the git SHA).
 */
export const APP_BUILD_ID = '20260923-protop-shell';
